# Game: Create a Number Guessing Game (user guesses random number).
"""
Created on Tue May 12 14:45:54 2026

@author: aarya more
"""

import streamlit as st
import random

st.title("🎯 Number Guessing Game")

# Initialize the secret number and attempts
if 'target' not in st.session_state:
    st.session_state.target = random.randint(1, 100)
    st.session_state.attempts = 0

st.write("I'm thinking of a number between **1 and 100**.")

# User input
guess = st.number_input("Enter your guess:", min_value=1, max_value=100, step=1)

if st.button("Submit Guess"):
    st.session_state.attempts += 1
    
    if guess < st.session_state.target:
        st.warning("Too low! Try again.")
    elif guess > st.session_state.target:
        st.warning("Too high! Try again.")
    else:
        st.success(f"🏆 Correct! It took you {st.session_state.attempts} attempts.")
        if st.button("Play Again"):
            del st.session_state.target
            st.rerun()

# Reset option
if st.button("Reset Game"):
    del st.session_state.target
    st.rerun()
    
